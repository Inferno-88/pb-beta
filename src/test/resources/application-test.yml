spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/pbdbtest"
    username: "d.diallo"
    password: ""
    driverClassName: "org.postgresql.Driver"
    data: "classpath:/sql/data-test.sql"
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        default_schema: test

